# Güvenlik

- **Asla** `BOT_TOKEN`, `ADMIN_PASSWORD`, `JWT_SECRET` veya `CHANNEL_ID` değerlerini git’e commit etmeyin.
- Yerel ayarlar yalnızca `.env` dosyasında tutulur (`.gitignore` içindedir).
- Token sızdıysa [@BotFather](https://t.me/BotFather) üzerinden `/revoke` ile yenileyin.
